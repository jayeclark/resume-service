import { Evaluator } from './Evaluator';
// Placeholder for evaluator that calls Sagemaker multimodel endpoint to retrieve score
class SagemakerEvaluator implements Evaluator {
  evaluateText() {
    return 0;
  }
}